from typing import Optional


def lambda_handler(event: dict, context: Optional[dict]) -> str:
    """handler

    Parameters
    ----------
    event : dict
    context : Optional[dict]

    Returns
    -------
    str
    """
    return "Hello World!!"
